spring:
  application:
    name: eureka-demo-client
server:
  port: 0
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
